# AI 执行提示词

> **用途**：把「OPPO 健康数据导出与分析」的任务交给其他 AI（Claude / Codex / 各类 Agent）代跑。
> 本文档提供两段可直接复制的提示词（完整流程 / 每日增量），按使用场景选用。
> **为什么需要它**：描述不当会让 AI 去做「手册介绍页」，并为了填满版面而**编造你的健康数据**。详见第四节。

**选择哪一段？**

| 场景 | 用哪段 | 前置条件 |
|---|---|---|
| 首次使用 / 换机 / 需要重新采集数据 | **提示词 A（完整流程）** | 无 |
| 已有 `DB\combined\database_decrypted.db`，只想出报告 | **提示词 B（每日增量）** | 数据已导出 |

---

## 替换清单（只需改这 2 处）

| 占位符 | 替换为 | 说明 |
|---|---|---|
| `<工作目录>` | 本项目的根目录绝对路径 | 即本文件所在目录（项目根）。例：`D:\oppo-health-data-toolkit` |
| `<模拟器目录>` | 雷电模拟器的安装目录 | 常见默认为 `C:\leidian\LDPlayer14`，实际位置随安装时的选择而异（盘符 / 目录都可能不同）。不确定就填你在多开器里看到的路径，或设环境变量 `OPPO_EMULATOR_DIR` |

> 本项目脚本支持自定位——**只要路径写对，目录改名或搬家都不影响运行**。

---

## 一、提示词 A：完整流程（采集 → 导出 → 生成报告）

```
请执行「OPPO 健康数据导出与分析」的完整流程，工作目录是 <工作目录>。

【参考资料】<工作目录>\OPPO健康数据导出与分析手册.md 与 <工作目录>\README.md
—— 这两份文档只用于理解步骤与排错，不是用来"做介绍网页"的素材。

【第一阶段：采集并导出我的真实健康数据】
严格按手册「⚡ 快速开始 → A. 一键执行」执行，不许跳过、不许简化、不许用模拟数据代替：
  1) 启动雷电模拟器（可执行文件在 <模拟器目录>\dnplayer.exe，或用
     <模拟器目录>\ldconsole.exe launch --index 0），等待模拟器完全开机
  2) 连接 ADB：<工作目录>\platform\adb.exe connect 127.0.0.1:5555
     （若 5555 连不上，按手册问题 10 改用 emulator-5554）
  3) 确认模拟器内 frida-server 已在运行（手册 1.5 节）；
     若 dist\frida-server 不存在，先运行 python tools\rejoin.py 还原分卷
  4) 先确认模拟器里 OPPO 健康 App 能正常启动、且已有近期数据
     （App 起不来见手册问题 8；数据停在昨天见问题 9）
  5) 在 <工作目录> 下运行：python auto_export_and_analyze.py
  6) 必须等到返回码为 0（2 = 导出数据不完整但报告已按现有数据生成；1 = 失败），并确认 <工作目录>\DB\combined\database_decrypted.db 已生成

【第二阶段：生成网页报告】
在 <工作目录> 下运行 python generate_html_report.py，产出 <工作目录>\OPPO健康数据分析报告.html
—— 这就是我要的网页（脚本会自动双写到 DB\combined\）。
必须用这个现成脚本，不要自己另写一个网页生成脚本。
报告结构与口径须符合本文档「三、报告规格」。

【硬性约束 —— 每一条都必须遵守】
- 网页里出现的每一个数值，都必须来自第一阶段导出的真实数据库
  （<工作目录>\DB\combined\database_decrypted.db）；
- 严禁编造、估算、用占位值（如 12345、示例、Lorem）或"看起来合理"的假数据填充版面；
- 如果导出失败、数据库为空、或 App 无近期数据：立即停止，如实报告卡在哪一步、
  报错是什么、你尝试了什么，不要为了"完成页面"而凑出一份看起来完整的报告；
- 产出后必须自检：用 SQL 直查 database_decrypted.db，随机抽 ≥3 个数值（建议 5 个，
  覆盖：昨日主睡时长、当日步数、日均压力值、最近体重、AHI 均值）与页面比对，
  全部一致方可交付；不一致必须先修正；
- 若页面上出现任何"来自文档而非我的数据"的数字（如文档记载的预期值、阈值、参考区间），
  必须在页面上显式标注来源，不得让它看起来像是我的数据；
- 所有生成物都放在 <工作目录> 内。

【最后请汇报】
1) 导出是否成功、返回码；2) database_decrypted.db 的大小与表数量；
3) 网页报告的文件路径；4) 你抽检的数值对照表；5) 任何未解决的报错。
```

---

## 二、提示词 B：每日增量（数据已导出，只生成报告）

```
请基于已导出的 OPPO 健康数据生成网页版分析报告。

【前置检查】
确认 <工作目录>\DB\combined\database_decrypted.db 存在
（若不存在，先按手册第二部分完成导出与解密）。该库由 App 版本决定表结构，
通常含约 100~101 张表（用户 100 张 + 内部 1 张 sqlite_sequence，后者仅在存在 AUTOINCREMENT 表时生成，故总数在 100~101 间浮动），其中非空表约 46~47 张，随 App 版本与设备传感器浮动。

【生成报告（主路径，首选）】
  cd /d <工作目录> && python generate_html_report.py
生成器自动完成：以报告锚点日（环境变量 OPPO_DATA_DATE 或本机当天）的**前一天**为"昨天"，
动态计算四个时间窗口 → 读取全部数据表 → 双写输出主文件
<工作目录>\OPPO健康数据分析报告.html 与数据源目录副本
<工作目录>\DB\combined\OPPO健康数据分析报告.html
（副本固定落盘 DB\combined\，该目录存在时；数据源优先级见手册 5.4）。
退出码 0 且打印两行"✅ 已写入"即成功。

【生成报告（备用路径）】
生成器缺失或报错且无法修复时，严格按本文档「三、报告规格」从零生成，
结构与口径不得简化；产出文件名须加后缀（如 OPPO健康数据分析报告_manual.html），
并在汇报中明确注明"非 generate_html_report.py 产出"，以便事后与真报告区分。

【数值验证（必须）】
随机抽取 ≥3 项报告数值（建议 5 项，覆盖：昨日主睡时长、当日步数、日均压力值、最近体重、AHI 均值），
用 SQL 直查 database_decrypted.db 独立复算比对；全部一致方可交付，不一致必须先修正。

【失败处理】
- 模拟器/Frida 未运行 → 按手册问题 2 启动模拟器、按 1.5 节启动 frida-server 后重新导出。
- 生成器报错 → 先读报错栈修复生成器本身（禁止绕过校验）；无法修复时走备用路径，
  并在汇报中明确注明。

【硬性约束】
- 页面上每一个数值都必须来自真实数据库，严禁编造或用占位值充数；
- 拿不到数据就停下如实报告，不要凑出一份"看起来完整"的报告；
- 页面上若出现文档记载的预期值/阈值，必须标注来源。

【完成汇报】
报告两份落盘路径 + 验证数值对照表 + 当日"需关注/待改善"条数。
```

---

## 三、网页报告规格（AI 自检与人工复核标准）

从零生成的《OPPO健康数据分析报告.html》必须与主生成器输出**结构一致、口径一致**，验收要点：

1. **单文件 HTML**：双击离线可开、无外部依赖；页头注明生成时间与数据锚点日。
2. **页面结构**：左侧导航（总览 + 四个时间窗口锚点）+ 顶部总览 tile + 四个时间窗口分区，每分区可展开明细。
3. **四个时间窗口**：以锚点日（`OPPO_DATA_DATE` / 本机当天）**前一天**为"昨天"，依次为 昨天 / 最近3天 / 最近7天 / 最近30天（均**不含锚点日当天**，避免当日尚未同步完整的数据混入）；某窗口无数据时必须显示提示条，并注明全库最新数据日。
4. **每窗口六模块**：健康建议、睡眠分析、心率分析、运动与活动、压力与恢复、体重变化——与主生成器 `_MODULES` 一致（**顺序不得调整**），含各模块评分与"需关注 / 待改善"计数。
5. **数值口径**：与手册第三部分一致——睡眠时长=分钟、心率=bpm、血氧=%、压力=0-100、运动取日汇总口径 `sport_mode=-2`。
6. **落盘与汇报**：双写 `<工作目录>\OPPO健康数据分析报告.html` 与 `<工作目录>\DB\combined\` 副本；汇报中给出两份路径 + 数值对照表 + 当日"需关注/待改善"条数。

---

## 四、防跑偏要点（为什么这样写）

| # | 要点 | 错误写法 | 正确写法 |
|---|---|---|---|
| 1 | **说清"给谁做页面"** | 「生成网页**展示该手册的**核心说明与分析方法」——宾语是**手册**，AI 会去做手册介绍页 | 「生成网页**展示我的健康数据**分析结果」——宾语是**我的数据** |
| 2 | **说清"执行什么"** | 「依据文档内容与流程进行**操作**」——"操作"没有目标 | 「**执行导出流程**：启动模拟器 → 连 ADB → 跑 `auto_export_and_analyze.py`」 |
| 3 | **说清"用什么命令"** | 不提生成方式，AI 可能自己写一个 | 「**运行 `generate_html_report.py`** 产出报告」——本项目网页报告有固定生成器 |
| 4 | **加反编造约束** | 只说"确保信息准确"——含义会被理解为"版面要完整" | 「数值必须来自真实数据库，**严禁编造**；拿不到就如实报告，不许凑数据」 |

> **核心**：AI 编造数据，通常不是因为它"想骗你"，而是因为**你的要求里没有真实数据的来源，却要求"内容完整"**——它只能用编造的示例把版面填满。
> 堵住这个口子只需说清两件事：**数据只能来自哪里** + **拿不到就停下**。

---

## 五、交付验收清单

AI 交付后逐条核对，任一条不满足就要求它重做：

- [ ] `DB\combined\database_decrypted.db` 真实存在，大小为数 MB（不是 0 字节）
- [ ] 报告文件是 `OPPO健康数据分析报告.html`，由 `generate_html_report.py` 产出（不是 AI 自写的页面）
- [ ] 页面里同时有「昨天 / 最近 3 天 / 最近 7 天 / 最近 30 天」四个时间窗口
- [ ] 页面**没有**出现示例值、占位串、明显凑数的整数（如 `12345`、`示例`、`Lorem`）
- [ ] 随机抽 ≥3 个数值（建议 5 个），用 `sqlite3` 或 Navicat 直查数据库，与页面一致
- [ ] 页面上出现的文档预期值/阈值已标注来源

---

## 六、常见跑偏与纠正

| 跑偏表现 | 原因 | 纠正话术 |
|---|---|---|
| 交付了一个介绍手册的网页 | 提示词宾语是"手册"而非"数据" | 「不要做手册介绍页，我要的是**我的健康数据报告**，由 `generate_html_report.py` 产出」 |
| 页面数值像是编的 | 没给死"数据来源"与"拿不到就停" | 「请先跑完导出并贴出 `database_decrypted.db` 的实际大小与表数量，再生成页面」 |
| 跳过导出直接写页面 | 没强调"先导出后生成"的顺序 | 「必须先把第一阶段跑通、返回码为 0，再进入第二阶段」 |
| 报告只有 44KB、四个窗口都显示"没有导出数据" | 旧版生成器曾优先读单日库（盲审 P1-1 已修：现默认优先 `DB\combined\` 全量库） | 现行版本不应出现此现象；若见到，先确认 `generate_html_report.py` 为最新版，再按手册 5.4 排查 |
| 5555 端口连不上 | 雷电 ADB 端口动态变化 | 按手册问题 10 改用 `emulator-5554` |
| App 起不来 / 数据停在昨天 | Zygisk 开启 或 检测到 Root | 按手册问题 8 跑 `disable_zygisk.sh`，用 `ldconsole quit` 重启（勿用 `adb reboot`） |

---

*本文件与《OPPO健康数据导出与分析手册》第九部分 9.6 节配套使用。*
