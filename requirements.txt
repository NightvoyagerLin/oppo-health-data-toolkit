# OPPO 健康数据导出与分析工具集 —— Python 依赖
#
# 安装：pip install -r requirements.txt
#
# 说明（全量审查修复 L9 · 2026-09-15）：
#   原清单只写包名、完全不限定版本，任何人任何时候安装都可能装到与实测不同的版本，
#   出问题难以复现。现按下列策略限定：
#     · frida —— **必须精确匹配**。它要与设备端 frida-server 同版本才能 attach；
#       本仓库 dist/ 分卷还原出的正是 17.17.0，故这里锁死 ==17.17.0。
#       换用其它版本时，请同时替换 dist/ 下的分卷。
#     · 其余 —— 给"实测通过的最低版本"作为下界（>=），既保证可复现基线，
#       又不阻塞上游的安全更新。
#   以下版本号均为 2026-09-15 在本机实测通过的那一组。

# ---------- 核心依赖（导出与本地分析主流程）----------
frida==17.17.0          # 必须与设备端 frida-server 同版本（dist/ 分卷为 17.17.0）
frida-tools>=14.10.4

# ⚠️ Windows 上 sqlcipher3 常因缺编译环境而 pip 安装失败。
#    优先尝试：pip install sqlcipher3
#    失败时改用预编译轮子：pip install sqlcipher3-binary
#    （两者提供同名 import，任选其一即可；Linux/macOS 一般可直接装 sqlcipher3）
sqlcipher3>=0.6.2

# ---------- 上传 / MCP 链路（可选：只做本地导出分析可不装）----------
flask>=3.1.3
requests>=2.34.2
mcp>=1.29.1

# ---------- GUI 导出 Excel 时需要（可选，缺失时 GUI 会提示安装）----------
# openpyxl>=3.1.0
